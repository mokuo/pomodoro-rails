const initialState = {
  type: null,
  object: null,
  projectId: null,
  taskId: null,
  pomodoroId: null
}

const operation = (state = initialState, action) => {
  return state
}

export default operation
